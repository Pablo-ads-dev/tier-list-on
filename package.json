{
  "name": "tierlist",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:esbuild": "esbuild src/pages/main-page/main-page.ts --bundle --outfile=dist/pages/main-page/main-page.js --format=esm --minify",
    "build": "npm run build:esbuild"
  },
  "devDependencies": {
    "esbuild": "^0.18.17",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "sweetalert2": "^11.21.2"
  }
}